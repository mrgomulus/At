<UserControl x:Class="OBD2Suite.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="{StaticResource BackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Live Data Dashboard" FontSize="22" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource SecondaryTextBrush}" FontSize="12" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Refresh:" VerticalAlignment="Center" Foreground="{StaticResource SecondaryTextBrush}" Margin="0,0,8,0"/>
                <ComboBox Width="90" SelectedValue="{Binding RefreshInterval}"
                          Style="{StaticResource BaseComboBoxStyle}">
                    <ComboBoxItem Content="500 ms"  Tag="500"/>
                    <ComboBoxItem Content="1000 ms" Tag="1000" IsSelected="True"/>
                    <ComboBoxItem Content="2000 ms" Tag="2000"/>
                    <ComboBoxItem Content="5000 ms" Tag="5000"/>
                </ComboBox>
            </StackPanel>
        </Grid>

        <!-- Toolbar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <Button Content="{Binding PollButtonLabel}" Command="{Binding TogglePollCommand}"
                    Style="{StaticResource AccentButtonStyle}" Padding="16,8" Margin="0,0,8,0" Width="150"/>
            <Button Content="↺  Refresh Once" Command="{Binding RefreshOnceCommand}"
                    Style="{StaticResource BaseButtonStyle}" Padding="16,8"/>
        </StackPanel>

        <!-- Live Data Grid -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="0">
            <DataGrid ItemsSource="{Binding LiveParameters}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False" IsReadOnly="True"
                      GridLinesVisibility="Horizontal"
                      Background="Transparent"
                      RowBackground="#0AFFFFFF"
                      AlternatingRowBackground="#05FFFFFF"
                      BorderThickness="0"
                      Foreground="{StaticResource PrimaryTextBrush}"
                      ColumnHeaderHeight="36"
                      RowHeight="40">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#2A2A3E"/>
                        <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="12,0"/>
                        <Setter Property="BorderBrush" Value="#3A3A5C"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="12,0"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
                        <Setter Property="Background" Value="Transparent"/>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="PID"         Binding="{Binding PidHex}"   Width="60"/>
                    <DataGridTextColumn Header="Parameter"   Binding="{Binding Name}"     Width="220"/>
                    <DataGridTextColumn Header="Category"    Binding="{Binding Category}" Width="120"/>
                    <DataGridTemplateColumn Header="Value" Width="160">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Value, StringFormat='{}{0:F2}'}"
                                               Foreground="{StaticResource AccentBrush}" FontWeight="Bold"
                                               FontFamily="Consolas" FontSize="14" Margin="12,0,4,0"/>
                                    <TextBlock Text="{Binding Unit}"
                                               Foreground="{StaticResource SecondaryTextBrush}" FontSize="11"
                                               VerticalAlignment="Bottom" Margin="0,0,0,1"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Bar" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid Margin="12,8">
                                    <Border Background="#1AFFFFFF" CornerRadius="3" Height="8"/>
                                    <Border Background="{StaticResource AccentBrush}" CornerRadius="3" Height="8"
                                            HorizontalAlignment="Left" MinWidth="0">
                                        <Border.Width>
                                            <Binding Path="BarWidth"/>
                                        </Border.Width>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Min"         Binding="{Binding MinValue}"  Width="60"/>
                    <DataGridTextColumn Header="Max"         Binding="{Binding MaxValue}"  Width="60"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Status -->
        <Border Grid.Row="3" Background="#1A00D4FF" CornerRadius="6" Padding="12,8" Margin="0,12,0,0">
            <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource PrimaryTextBrush}" FontFamily="Consolas" FontSize="12"/>
        </Border>
    </Grid>
</UserControl>
