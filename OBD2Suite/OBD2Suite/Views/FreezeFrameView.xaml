<UserControl x:Class="OBD2Suite.Views.FreezeFrameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="📸 FREEZE FRAME DATA (MODE 02)" Style="{StaticResource HeadingText}" Margin="0,0,0,16"/>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <Button Content="📸 Read Freeze Frame" Command="{Binding ReadCommand}"
                        Style="{StaticResource PrimaryButton}" Margin="0,0,8,0"/>
                <TextBlock VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"
                           Text="{Binding StatusMessage}" FontFamily="Consolas" FontSize="12"/>
                <ProgressBar IsIndeterminate="True" Width="120" Height="6" Margin="12,0,0,0" VerticalAlignment="Center"
                             Foreground="{StaticResource AccentBrush}">
                    <ProgressBar.Style>
                        <Style TargetType="ProgressBar">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ProgressBar.Style>
                </ProgressBar>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding FreezeFrames}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,12">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>
                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                                    <TextBlock Text="📸" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Title}" Style="{StaticResource SubheadingText}"/>
                                    <TextBlock Text="{Binding CapturedAt, StringFormat=' — {0:dd.MM.yyyy HH:mm:ss}'}"
                                               Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                                <DataGrid Grid.Row="1" ItemsSource="{Binding Parameters}"
                                          AutoGenerateColumns="False" CanUserAddRows="False"
                                          Background="Transparent" BorderThickness="0"
                                          RowBackground="Transparent" AlternatingRowBackground="#0AFFFFFF"
                                          Foreground="{StaticResource TextPrimaryBrush}">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="PID"        Binding="{Binding PidHex}"    Width="60"  IsReadOnly="True"/>
                                        <DataGridTextColumn Header="Parameter"  Binding="{Binding Name}"      Width="*"   IsReadOnly="True"/>
                                        <DataGridTextColumn Header="Value"      Binding="{Binding DisplayText}" Width="120" IsReadOnly="True"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
